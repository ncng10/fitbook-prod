mutation AddExerciseToWorkout($input: NewExerciseInput!) {
  addExerciseToWorkout(input: $input) {
    id
    workoutIdentity
    exerciseName
    weight
    sets
    reps
    time
    rpe
    notes
  }
}
