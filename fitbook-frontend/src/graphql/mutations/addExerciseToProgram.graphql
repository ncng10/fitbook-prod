mutation AddExerciseToWorkout($inputs: NewExerciseInput!) {
  addExerciseToWorkout(inputs: $inputs) {
    id
    workoutIdentity
    exerciseName
    weight
    sets
    reps
    time
    rpe
    notes
  }
}
