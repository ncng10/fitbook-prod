mutation CreateWorkout($input: CreateWorkoutInput!) {
  createWorkout(input: $input){
    id
    programIdentity
    workoutDate
    creatorId
    workoutName
    isShared
    workoutCompleted
    workoutCategory
  }
}